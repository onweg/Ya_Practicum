#include <iostream>

using namespace std;

enum class DocumentStatus {
    ACTUAL,
    IRRELEVANT,
    BANNED,
    REMOVED,
};

class SearchServer {
public:
    // перенесите сюда DocumentStatus
};

int main() {
    // выведите численное значение статуса BANNED
}