struct Document {
	int id;
	int relevance;
};

/* возвращаемое значение */  HasDocumentGreaterRelevance(/* параметры функции */) {
		return /* условие, при котором первый объект будет в векторе раньше второго */;
}

int main() {

    return 0;
}